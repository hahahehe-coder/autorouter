# python
.venv/
__pycache__/
*.pyc

# 前端依赖；web/dist 作为发布时管理界面的一部分入库
web/node_modules/

# 运行时日志(宿主挂载,不入库)
log/

# ML bundle(~76MB 二进制,从 OpenSquilla 拷入;不入库,各环境自行放置/配置 bundle_path)
# models/

# 本地杂物
.claude/
